{
  "name": "murmur-copilot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "npx serve src --listen tcp://localhost:1420 --no-clipboard",
    "build": "echo 'No build step — vanilla TS/HTML frontend'"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
